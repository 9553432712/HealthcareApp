# postgres config map object

apiVersion: v1
kind: ConfigMap
metadata:
  name: healthapp-config
  labels:
    app: healthapp
data:
  SPRING_DATASOURCE_USERNAME: postgres
  SPRING_DATASOURCE_PASSWORD: postgres
  SPRING_DATASOURCE_URL: postgres-service